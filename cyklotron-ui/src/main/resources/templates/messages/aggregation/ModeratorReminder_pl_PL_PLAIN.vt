W module agregacja w poniższych serwisach znajdują się następujące zadania:

#foreach($site in $sites)
Serwis: $site.name
Liczba otrzymanych rekomendacji: $pendingMap.get($site).size()
Liczba złożonych rekomendacji: $submittedMap.get($site).size()
Liczba zasobów zmodyfikowanych: $changedMap.get($site).size()
----------------------------------------------
#end

Administrator systemu.
