#foreach($cmsData.site in $sites)
#if($rootNodes.containsKey($site))
<a href="$link.unsetView().set('x', $rootNodes.get($site).id)">$cmsData.site.name</a> <br/>
#else
$cmsData.site.name - broken <br/>
#end
#end
