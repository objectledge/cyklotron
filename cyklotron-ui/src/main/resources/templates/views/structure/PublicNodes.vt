<h4>
pages available to user $cms_data.userData.login in site $cms_data.site.name
<h4>
	
#foreach($node in $visible)
<a href="$link.unsetView().unset('site_id').set('x', $node.id)">$node.name</a><br/>
#end
