#genericScreenHeader('KONFIGURACJA KOMPONENTÓW GLOBALNYCH')
#foreach($instance in $components)
$component.embed('cms','CMSComponentWrapper', [['instance', $instance]])<br/>
#end
#genericScreenTrailer()
