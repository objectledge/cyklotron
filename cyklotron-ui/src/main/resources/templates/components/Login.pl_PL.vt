<script type="text/javascript">
<!--
	function focus() {
		if (document.loginform.login) {
			document.loginform.login.focus();
		}
	}
	window.onload = focus;
// -->
</script>


<form name="loginform"##
    id="loginform" 
    action="$link.view($originalView).action("authentication.Login")"##
    method="post" class="defaultForm">
#set($syskeys = ['view','action','app'])
#foreach($key in $parametersTool.parameterNames)
#if(!$syskeys.contains($key))
<input type="hidden" name="$key" value="$stringTool.wrap($htmlEncoder.encodeAttribute($parametersTool.get($key),true),2048)"/>
#end
#end
<table width="100%" class="genericComponent" cellspacing="0">
  <tr>
    <td>Login:</td><td><input id="login" type="text" name="login" class="login-input" /></td>
  </tr>
  <tr>
    <td>Hasło:</td><td><input type="password" name="password" class="login-input" /></td>
  </tr>
  <tr>
    <td align="center" colspan="2"><input type="submit" class="submitbutton" value="Zaloguj" class="login-button" /></td>
  </tr>
 <tr>
</table>
## Załóż nowe <a href="$link.view('security.AddUser')">konto</a>
</form>
