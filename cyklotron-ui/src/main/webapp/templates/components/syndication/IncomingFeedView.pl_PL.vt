

<h1>$feed.name</h1>
<p>
$feed.contents
</p>
<p>
Źródło: $feed.url
<br />
Ostatnie pobranie:
#if($feed.lastUpdate)
$format_date.format($feed.lastUpdate)
#if($feed.failedUpdates > 0) <span class="warning">(failed)</span>#end
#end
</p>
